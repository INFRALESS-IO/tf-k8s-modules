apiVersion: v1
data:
  #echo -n "CHANGEMECHANGEMECHANGEME" | base64 | base64
  keyfile: {{ required "mongodb.mongoKey is required in chart values" .Values.mongodb.mongoKey | b64enc }}

kind: Secret
metadata:
  name: mongo-key
  namespace: {{ required "mongodb.namespace is required in chart values" .Values.mongodb.namespace }}

type: Opaque
